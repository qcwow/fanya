# 开发环境配置
springdoc:
  paths-to-match: /api/**
  swagger-ui:
    operations-sorter: alpha

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: qcw050110QCW@
    driver-class-name: com.mysql.cj.jdbc.Driver
  security:
    jwt:
      key: 'abcdefghijklmn'
      expire: 72
      limit:
        base: 1
        upgrade: 300
        frequency: 100
    filter:
      order: -100
  web:
    flow:
      period: 3
      limit: 50
      block: 30
    cors:
      origin: '*'
      credentials: false
      methods: '*'
fanya:
  api:
    static-key: '123456' # 这里填你和超星商定的密钥